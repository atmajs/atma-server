import { mask, Uri } from '../dependency'

let initializeEmbeddedComponents = function (app) {
	initializeEmbeddedComponents = function(){};

	let DEBUG = app.config.$is('debug'),
		BUILD_DIR = '/public/build/'
		;

	// import resources/exports.js
};

export { initializeEmbeddedComponents };
